1.Как организовать клиент-серверное взаимодействие?
2.Как и в каком виде передавать файлы?
3.Как пересылать большие файлы?
4.Как пересылать служебные команды?
5.Что хранить в базе данных?
6.Как передавать структуру каталогов/файлов?
7.Какую библиотеку использовать для сетевого взаимодействия: java.io, java.nio, Netty?


1. В формате запросов и ответов (request и response). Протокол разрабатывается индивидуально;

2. Java object, Json, xml. Первый - наиболее предпочтителен (Serialisation: object inputstream, object outputstream) в байтах. Как альтернатива,
можно самостоятельно реализовать байт протокол;

3. Список из строк заворачивается в объект;

4. Служебные команды пересылаются зашифрованными или окруженные неким символом, к примеру #addFile#;

5. Предполагаю, что в базе хранятся данные пользователя для авторизации (логин, пароль, ник?);



